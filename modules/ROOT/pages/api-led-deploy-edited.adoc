= Step 5. Deploy the API to CloudHub 2.0

Now that you’ve tested your API locally, deploy it to CloudHub 2.0 so it’s accessible from anywhere.

== Step 5.1: Prepare for Deployment

CloudHub 2.0 requires a release version (not a snapshot). When you scaffolded the project, Anypoint Code Builder created a `pom.xml` with `-SNAPSHOT` in the version (Maven’s convention for work-in-progress code). Remove it before deploying.

. In Anypoint Code Builder, open `pom.xml` in your project.

. Find the `<version>` element and remove `-SNAPSHOT`:
+
[source,xml]
----
<groupId>com.mycompany</groupId>
<artifactId>hello-world-api</artifactId>
<version>1.0.0</version>
<packaging>mule-application</packaging>
----

. Save the file.

== Step 5.2: Deploy to CloudHub 2.0

. Open `hello-world-api.xml` in the editor.

. Click the image:icon-deploy.png["",20,20] (*Deploy to CloudHub*) icon.
+
Alternatively, open the Command Palette and select `MuleSoft: Deploy to CloudHub`.

. If prompted, click *Allow* to allow sign in using Anypoint Platform and select the business group.

. Select a Business Group to use for deployment.

. When prompted via the VS Code command palette, select *CloudHub 2.0* as the deployment target.
+
You are prompted to select the space to deploy your application.

. If the project doesn't already have a `deploy_ch2.json` file, Anypoint Code Builder indicates that a deployment configuration file has been created and opens it for you to review:
+
[source,json]
----
{
  "runtime": "4.8.0", <1>
  "applicationName": "hello-world-impl-jsmith", <2>
  "replicas": 1, <3>
  "replicaSize": "0.1", <4>
  "deploymentModel": "rolling", <5>
  "autoStart": true, <6>
  "ObjectStoreV2": true <7>
}
----
<1> The Mule runtime version to use for the deployment.
<2> Defines part of the URL for accessing your application on CloudHub. Must be unique across all applications deployed to CloudHub. Update this to include your initials or another unique identifier.
<3> Number of application instances to run. Use 1 for this tutorial; increase for production workloads.
<4> The vCore allocation per replica. 0.1 vCores provides 0.5 GB of memory.
<5> The deployment strategy. Use `rolling` for zero-downtime updates, or `recreate` for a full restart.
<6> Starts the application automatically after deployment.
<7> Enables Anypoint Object Store v2 for storing application state.

. After reviewing the deployment configuration, click *Deploy* to deploy the application to CloudHub 2.0.
+
The VS Code status bar shows the progress of the deployment.

. At the prompt, select the environment to deploy to.
+
Anypoint Code Builder packages the application and deploys it to CloudHub 2.0.
The Output panel opens with *Mule Maven Output* selected:
+
After a couple of minutes, you'll see `Deployment successful`.

. Click *Open in Runtime Manager*.

== Step 5.3: Verify in Runtime Manager

. Runtime Manager opens showing your application.

. Wait for the status to change from *Deploying* to *Running* (this takes a few minutes).

. After the app is running, copy the application URL.
+
The format is: `+https://hello-world-impl-jsmith.<space-domain>.cloudhub.io+`

. Open Postman and test your deployed API:
+
[source,text]
----
GET https://hello-world-impl-jsmith.<space-domain>.cloudhub.io/api/greeting?name=World
----

. Verify you receive your greeting:
+
[source,json]
----
{
  "message": "Hello, World!"
}
----

Congratulations! Your API is live in the cloud!

== Step 5.4: What Just Happened?

Let's understand the tools you used:

* CloudHub 2.0 is MuleSoft's cloud platform where your API now runs
* A Shared Space is a multi-tenant environment for cost-effective hosting.
* The `deploy_ch2.json` configuration file specifies replicas, resources, deployment model, and and other settings.
+
Replicas are instances of your application (one for this tutorial).
+
The rolling deployment model is a zero-downtime deployment strategy.
* Runtime Manager is the operations dashboard for deployed applications.

== What's Next

Now that your API is deployed, learn how to monitor it and view logs in Runtime Manager.

xref:api-led-operate-edited.adoc[Next: Monitor and Manage]


== Developer Deep Dive

Explore advanced CloudHub 2.0 capabilities:

* xref:cloudhub-2::ch2-architecture.adoc[]
* xref:cloudhub-2::ch2-shared-space-about.adoc[]
* xref:cloudhub-2::ch2-update-apps.adoc[]
* xref:cloudhub-2::ch2-configure-private-space.adoc[]
* xref:api-manager::policies-landing-page.adoc[]
