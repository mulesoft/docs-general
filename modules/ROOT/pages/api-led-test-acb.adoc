= Step 4. Test the API

In this step, you'll test your API using Anypoint Code Builder's debugging tools and add validation and logging to see what's happening inside your application.

== Step 4.1: Run and Test Your API

. Open the Run and Debug panel.
+
.Show me how
[%collapsible]
====
** Click the image:icon-run-debug.png["",15,15] (*Run and Debug*) icon in the activity bar.
* Use the keyboard shortcuts:
+
*** Mac: Cmd+Shift+d
*** Windows: Ctrl+Shift+d
** In the desktop IDE, select *View* > *Run*.
====
The Output panel shows startup progress. Wait for `Application started successfully`.

. Open Postman and send a GET request:
+
[source,text]
----
GET http://localhost:8081/api/greeting?name=World
----

. Verify you receive:
+
[source,json]
----
{
  "message": "Hello, World!"
}
----
+
Your API works! Now let's add validation and debugging.

. Click *Stop* in the debug toolbar.

== Step 4.2: Add Validation

Now add validation to ensure your API handles requests without a `name` parameter.

. In the canvas, navigate to your *get:\greeting* flow.
. Click the image:icon-plus.png["",15,15] (*Add component*) icon *before* the *Logger* component.
. Search for *Choice* and select it from the results.
+
The Choice component adds a *When* branch and an *Otherwise* branch. Execution continues to Logger and Transform after the Choice unless the When branch stops it (with Raise Error).
. Click the image:icon-plus.png["",15,15] (*Add component*) icon inside the *When* branch.
. Search for *Raise error* and select it.
+
[IMPORTANT]
Add the *Raise Error* component to the *When* branch before setting the condition. The *When* branch must contain at least one component or the XML will be invalid.
. Configure the *Raise Error* component:
+
* *Type*: `APP:BAD_REQUEST`
* *Description*: `Name parameter is required`
. Click the *Choice* component to open its configuration panel and set the *When* condition to:
+
[source,dataweave]
----
#[attributes.queryParams.name == null or attributes.queryParams.name == ""]
----
. Save your changes.
+
Your flow order is now: *Choice* → *Logger* → *Transform*. When `name` is missing, the When branch raises an error and the flow stops. When `name` is present, execution continues past the Choice to Logger and Transform.

== Step 4.3: Test the Validation

. Start your application using Run and Debug.

. In Postman, test with a valid name parameter:
+
[source,text]
----
GET http://localhost:8081/api/greeting?name=World
----
+
You should receive: `{"message": "Hello, World!"}`

. Test without the name parameter:
+
[source,text]
----
GET http://localhost:8081/api/greeting
----
+
You should receive a validation error with status `400`.

. Click *Stop* in the debug toolbar.

Your API now validates input before processing.

== Step 4.4: Add Logging

Logging helps you see what's happening inside your flows.

. In the canvas, find your *get:\greeting* flow.

. Click the *Logger* component to configure it:
+
* *Message*: `#["Received greeting request for name: " ++ (attributes.queryParams.name default "not provided")]`
* *Level*: INFO

. Save your changes.

== Step 4.5: Test with Logging

. Click *Start Debugging (F5)* to start your application.

. Open the Terminal panel to see your application logs:
+
* Mac: Cmd+\` or select *View* > *Terminal*
* Windows: Ctrl+\` or select *View* > *Terminal*
+
[NOTE]
Logger output appears in the Terminal panel, not the Output panel. The Terminal shows the live Mule runtime log as requests are processed.

. Send a request with Postman:
+
[source,text]
----
GET http://localhost:8081/api/greeting?name=World
----

. In the Output panel, select *Mule Maven Output*.
+
In the Terminal panel, you'll see a log entry similar to:
+
[source,text]
----
INFO  [hello-world-impl].get:\greeting  Received greeting request for name: World
----

. Send a few more requests and watch the logs update.

. Click *Stop*.

== Step 4.6: Use Breakpoints for Debugging

Breakpoints pause execution so you can inspect what's happening.

. In the canvas, click in the left margin next to the Logger component.
+
A red dot appears, which represents a breakpoint.

. Open the Run and Debug panel.
+
.Show me how
[%collapsible]
====
** Click the image:icon-run-debug.png["",15,15] (*Run and Debug*) icon in the activity bar.
** Use the keyboard shortcuts:
+
*** Mac: Cmd+Shift+d
*** Windows: Ctrl+Shift+d
** In the desktop IDE, select *View* > *Run*.
====

. Click *Debug* (not Run) to start in debug mode.

. Send a request from Postman.

. Return to Anypoint Code Builder.
+
Execution pauses at the breakpoint. The debug toolbar appears at the top.

. In the Variables panel (bottom), expand *message* to see:
+
* *payload*: The current message content
* *attributes*: HTTP request details (headers, query params, URI)
* *vars*: Flow variables

. Click *Continue* (or press F5) to proceed to the next breakpoint.

. Click *Continue* again to complete the request.

. Click *Stop* when done.

== What Just Happened?

You used Anypoint Code Builder's testing and debugging tools:

* *API Console*
+
Quick testing without leaving the IDE
* *Validation Module*
+
Ensures your API handles invalid input properly
* *Logger*
+
Adds visibility into your flows
* *Breakpoints*
+
Pauses execution to inspect data
* *Variables Panel*
+
Shows message content at each step
* *Debug Mode*
+
Step through your flow execution

== What's Next

With your APIs fully tested locally, you'll deploy them to CloudHub 2.0 to make them accessible to other applications and users.

xref:api-led-deploy-acb.adoc[Next: Deploy to CloudHub 2.0]

== Developer Deep Dive

Explore advanced testing capabilities:

* xref:anypoint-code-builder::int-test-munit.adoc[Automated Testing with MUnit]
* xref:mule-runtime::intro-error-handlers.adoc[Advanced Error Handling]
* xref:anypoint-code-builder::troubleshoot-gen-ai.adoc[Troubleshooting with MuleSoft Vibes]
* xref:mule-runtime::dataweave-testing-framework.adoc[DataWeave Testing Framework]
