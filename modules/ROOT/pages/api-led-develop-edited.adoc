= Step 3. Develop the API

Now that you've created an API specification, you'll turn it into a working Mule application. Anypoint Code Builder includes an embedded Mule runtime for local testing. Later, you'll deploy your application to xref:cloudhub-2::ch2-deploy.adoc[CloudHub 2.0], where it runs on Mule runtime in the cloud.

== Step 3.1: Create the Implementation Project

. In Anypoint Code Builder, open the Command Palette.
+
.Show me how
[%collapsible]
====
* Use the keyboard shortcuts:
** Mac: Cmd+Shift+p
** Windows: Ctrl+Shift+p
* In the desktop IDE, select *View* > *Command Palette*.
====

. Type and select: *MuleSoft: Develop an Integration*.

. Select *Create New Project*.

. Configure the project:
+
* *Project Name*: `hello-world-impl`
* Select *Import API Specification*: *From Exchange*.
+
NOTE: If your spec isn't in Exchange, select *From Local File* and browse to `hello-world-api.yaml`
* Enable *Scaffold flows from this API specification*

. Click *Create Project*.
+
Anypoint Code Builder generates:

* HTTP Listener configuration (receives requests)
* APIkit Router (routes to your endpoint)
* A flow for your GET `/greeting` endpoint
* Error handlers

. In the Explorer, open `hello-world-api.xml`.
+
The canvas displays your scaffolded flows.

== Step 3.2: Understand the Scaffolded Structure

. In the canvas, you'll see these flows:
+
* *hello-world-api-main*: HTTP Listener that receives requests on port 8081
* *get:\greeting:hello-world-api-config*: Your GET endpoint implementation
* Error handling flows

. Click the *HTTP Listener* component to review:
+
* *Path*: `/api/*` (receives all API requests)
* *Port*: `8081` (local development port)

== Step 3.3: Implement the Hello World Logic

Now add the logic to return your greeting.

. Navigate to the *get:\greeting:hello-world-api-config* flow in the canvas.

. The flow contains a *Transform Message* component. Click it.

. Replace the DataWeave code with:
+
[source,dataweave]
----
%dw 2.0
output application/json
---
{
  message: "Hello, World!"
}
----
+
This DataWeave script returns a simple JSON response with your greeting.

. Save your changes (Cmd+S on Mac, Ctrl+S on Windows/Linux).

That's it! Your API is implemented.

== Step 3.4: Test Locally

. Open the Run and Debug panel.
+
.Show me how
[%collapsible]
====
** Click the image:icon-run-debug.png["",15,15] (*Run and Debug*) icon in the activity bar.
** Use the keyboard shortcuts:
+
*** Mac: Cmd+Shift+d
*** Windows: Ctrl+Shift+d
** In the desktop IDE, select *View* > *Run*.
====

. Click *Run* to start your application.
+
The Output panel shows startup progress. Wait for "Application started successfully".

. Open Postman and send a GET request:
+
[source,text]
----
GET http://localhost:8081/api/greeting
----

. Verify you receive:
+
[source,json]
----
{
  "message": "Hello, World!"
}
----

. Click *Stop* in the debug toolbar.

Congratulations! Your API works locally.

== What Just Happened?

Let's understand what you used:

* *Scaffolding*: Anypoint Code Builder generated the project structure from your specification
* *HTTP Listener*: Receives incoming HTTP requests on port 8081
* *APIkit Router*: Routes requests to the correct endpoint flow
* *DataWeave*: MuleSoft's transformation language for creating responses
* *Canvas*: Visual editor for building flows
* *Run and Debug*: Built-in testing environment

== What's Next

Your API works on your machine. Next, you'll add a quick validation step to see how to test locally.


== Developer Deep Dive

Explore advanced implementation techniques:

* xref:mule-runtime::dataweave.adoc[DataWeave Language Reference]
* xref:connectors::database/database-connector.adoc[Database Connector]
* xref:mule-runtime::error-handling.adoc[Error Handling in Mule 4]
* xref:mule-runtime::about-flows.adoc[Understanding Flows and Subflows]
